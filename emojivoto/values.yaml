# Default values for emojivoto.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: "buoyantio"
  tag: v5
  pullPolicy: IfNotPresent
